# Decisions

What was decided, why, and by whom. The owner is Casimir; "planner" means Claude, acting as orchestrator, within
the owner's direction. Newest last.

## Direction

| Date | Decision | Why | By |
|---|---|---|---|
| 14 Sep 2026 | Rebuild Odoo's apps **from the ground up** rather than extend the existing Nocoly Sales app | The Sales app (`3e596740…`) mirrors one Odoo app with its own Contacts and Products; a foundation shared by every app has to come first | owner |
| 15 Sep 2026 | **One master app, ERP Master**, holds every Odoo app. A client gets a copy with the menu groups it does not need deleted | Delivery is duplicate-and-prune, so everything must coexist in one app; it also removes the need for cross-app Relations | owner |
| 15 Sep 2026 | **Optional feature bundles are excluded for now.** Phase 1 is the seven core worksheets — Contacts, Units & Packagings, Products, Product Variants, Journals, Invoices, Invoice Lines — built one at a time | Bundles (Taxes, Chart of Accounts, Categories, Pricelists, Geography…) can be appended later without touching what exists; core worksheets cannot | owner |
| 16 Sep 2026 | **Six bundle worksheets join Phase 1 after all**, built one at a time with the usual loop and in this order: **1 Product Categories · 2 Chart of Accounts · 3 Payment Terms · 4 Countries · 5 States · 6 Taxes**. This reverses the *recorded and ignored* rule above **for these six only** — every other bundle stays recorded and ignored | Asked for after Phase 1's core was closed out. The order is the dependency order the plan already carried: Taxes needs Countries (`account.tax.country_id` is `required=True`), States needs Countries, and the other three stand alone. Each replaces a stand-in or fills a gap the core left — Products gains a Category, Journals its accounts, Invoices real Payment Terms, Contacts real Country and State, and Invoice Lines its Taxes | owner |
| 16 Sep 2026 | **When a bundle replaces a text stand-in, add the relation and delete the old Text control** — the standing answer for all six, so it is not asked again per bundle. The order is always: create the relation, **carry the stand-in's values into it**, read them back, and only then delete the text field; afterwards re-write any view sort or column that pointed at the dead control (`BUILDING.md`) | Asked for by the owner, 16 Sep. Two text stand-ins fall under it: Invoices' **Payment Terms** (bundle 3) and Contacts' **Country** and **State** (bundles 4 and 5). Invoices' **Recipient Bank** does *not* — bank accounts are `res.partner.bank`, which is not among the six, so it stays text. Chart of Accounts and Taxes replace nothing: they add relations the core never had | owner |
| 16 Sep 2026 | **Product Categories' # Products counts the category's own products, not its subtree**, and **Odoo's recursion constraint is not built** | Odoo's help for `product_count` says it *"Does not consider the children categories"* while `_compute_product_count` counts the whole subtree — the two disagree, and a HAP roll-up can only do the first, so the help won. Goods therefore reads 0 where the tenant reads 11, and the Child Categories list beside it carries the four numbers that make up the 11. On recursion: no HAP rule can ask whether a record is its own ancestor, and a guard that caught only the trivial case would read as protection that is not there. Both are §3 differences with the evidence, and **both are the owner's to overturn** — each is a small workflow | planner |
| 17 Sep 2026 | **Chart of Accounts brings every account field in the app, and none of them is required on the field.** Contacts, Products, Product Categories, Journals and Invoice Lines gain their accounts, seeded with the tenant's values; the fields are hidden by role exactly as Odoo's views hide them (Invoicing loses all twelve, Accounting Read-only a contact's two), with the tabs they leave empty. Only Journals requires its accounts, by rule. An invoice line's account is filled by automation B, and the journal's Default Account fills only an empty account, as Odoo's compute does | The rule of bundle 1 — a bundle brings the fields that point at its model — applied to the model half the app points at. Odoo requires a contact's accounts and a line's account but hides them from part of the accounting staff, and a HAP field that is required yet hidden from a role blocks that role's saves, so defaults, the seed and the automation keep them filled instead; Journals is edited only by the two roles that see its accounts. The journal fallback first overwrote an existing account on a Product change and was aligned with Odoo on 17 Sep. A role *can* hide a single field in HAP, which is what made Odoo's group visibility buildable | planner |
| 16 Sep 2026 | **Bundles are recorded and ignored**: nothing a bundle would bring gets a stand-in field. It is named in the worksheet's *Not built now* and in the tab's remark block, and that is all | Asked for while starting 07 Invoice Lines. A stand-in has to be found and removed later, and it makes a worksheet read as if the feature were built; the earlier text stand-ins on 06 (Payment Terms, Recipient Bank) came from Teh Li Wei's skeleton and stay as they are | owner |
| 15 Sep 2026 | **Menu groups follow Odoo's apps**: Contacts · Products · Invoicing for Phase 1 | Mirrors how Odoo splits its menus, and lets a client copy drop an app by deleting one group | owner (asked for "the best logic, or how Odoo does it") |
| 15 Sep 2026 | **The reference is the live tenant casimir.odoo.com (Odoo saas~19.4)**, not the 19.0 checkout in this repo. Fields, labels, layout, views and records follow the tenant; behaviour it cannot show (constraints, computes, sync logic) is read from the 19.0 source | The tenant is what the business actually uses; 19.4 differs from 19.0 (Contacts lost its Person/Company switch, for example) | owner |
| 15 Sep 2026 | Names and labels in **English, as in Odoo** | A reviewer can compare Odoo and Nocoly side by side | owner |
| 15 Sep 2026 | Every worksheet gets a **hand-off** — `worksheets/NN-name.md` plus a published review page — checked by a colleague | Review happens independently of the build | owner |
| 15 Sep 2026 | **One implementation agent at a time** builds a worksheet (requirements + hap CLI build); Claude plans, reviews its work, runs the UI test, writes the hand-off and commits | Throughput with one point of review | owner |
| 15 Sep 2026 | **Worksheet 05 Journals is built by the colleague, in parallel.** Implementation agents skip it — after Products they do 04 Product Variants, then 06 Invoices and 07 Invoice Lines once Journals stands — and never touch the Journals worksheet | Two builders working at once; Invoices relates to Journals | owner |
| 15 Sep 2026 | **Deleting anything needs the owner's approval.** Mistakes are renamed "ZZ obsolete – " and listed; test records are named `TEST …` and removed after sign-off | Several HAP deletions cannot be undone | owner |
| 15 Sep 2026 | **ohyes.odoo.com is Teh Li Wei's own site; casimir.odoo.com stays the reference** for every worksheet, Journals included | One reference for the whole app; `reference/odoo-19.4/account.journal.md` is the casimir extract | owner |
| 15 Sep 2026 | **Teh Li Wei builds Journals (05) and Invoices (06) first.** When he is done with each, we re-check it against casimir, close the gaps, run the UI test and send him a summary. Until then implementation agents leave both worksheets alone; 07 Invoice Lines follows Invoices | Two builders without collisions: the colleague edited Journals and created Invoices while a gap-closing agent was preparing changes, which it parked unapplied (branch `claude/journals-gaps`) | owner |
| 15 Sep 2026 | Merging Teh Li Wei's branch `codex/journals-phase1-handoff` into 19.0 waits until the gaps are closed and double-checked | Keeps 19.0 matching what has been reviewed | owner |
| 17 Sep 2026 | **Payment Terms' two roll-up rules are built and disabled** — the percentages adding to 100, and an early discount needing a single line | A HAP 汇总 over a 子表 holds the saved rows only, so both judged a new term as empty and refused every one, its default 100 % row included (10 §3, `BUILDING.md`). Disabling keeps them ready for the day HAP's roll-ups follow unsaved rows; nothing checks the percentages meanwhile | planner |
| 17 Sep 2026 | **HAP's stock controls, dropped by the first save of a brand-new worksheet, do not count as a deletion** needing approval | They are the placeholders (Description, Attachment, Name) HAP puts on a worksheet the build created seconds earlier, never part of the app and never holding data; 08, 09 and 10 were all built this way | owner |
| 17 Sep 2026 | **An invoice created with a term and no Customer / Vendor is dated by a workflow of its own** (`6aab9e0d…`), which §1 did not name | HAP's *created or updated* trigger narrowed to Customer / Vendor does not fire for a create that leaves it empty; Odoo's own due date needs no partner, so the case is Odoo's, not an invention | owner |
| 17 Sep 2026 | **Each invoice is dated once**: automation C and Confirm are set to start no other workflow (`triggerType` 2), rather than having their dating steps removed | The owner asked for one of the two runs to go. The switch deletes nothing, keeps each workflow able to date a document it does not write to, and is read back and checked (`payterms.py check`) | owner |
| 17 Sep 2026 | **Products' Favorite is rebuilt after another administrator deleted it** (`6aabf880e43d174ab374dcf0`), and the three Products views and three Product Variants workflows re-pointed at the new control | `is_favorite` is a real Odoo field — the star beside the product's name, and Odoo's own list order. The deleted control stays in HAP's recycle bin and must not be restored, or the form would carry two Favorite checkboxes with one alias | owner |

## Open — to settle before the worksheet that needs it

| Needed by | Question | Recommendation from the plan |
|---|---|---|
| ~~07 Invoice Lines~~ | ~~Do lines point at Product Variants or Products?~~ | **Settled 16 Sep 2026** by the build: the Product field is a Relation to **Product Variants**. Every order line, stock move and invoice line in Odoo points at the variant; pointing at the template is the one choice that costs a rebuild rather than an append |
| ~~06 Invoices~~ | ~~One Invoices worksheet split by Move Type, or separate Bills?~~ | **Settled 16 Sep 2026** by the owner: one worksheet, all seven of Odoo's move types, three views dividing the table. See *Worksheet 06 · Invoices* below |

## Worksheet 01 · Contacts

| Date | Decision | Why | By |
|---|---|---|---|
| 15 Sep 2026 | **Tax ID stays on Contacts**; SST, TTx and the Malaysian TIN wait for the Taxes bundle and e-invoicing | Tax ID (`vat`) is a base field of every contact. SST and TTx come from `l10n_my_ubl_pint` and the TIN from `l10n_my_edi`, both built on Invoicing | planner, confirmed with owner |
| 15 Sep 2026 | Upstream sync (a contact's edit rewriting its company) is left out | Avoids automation loops; edit the company instead | planner |
| 15 Sep 2026 | **Show "Company, Person" as the display name, like Odoo** | Customer pickers on Invoices must identify the company | owner |
| 15 Sep 2026 | Delete the three rules the 19.0 build left disabled | They referred to a deleted field and did nothing | owner |
| 15 Sep 2026 | Display Name is a function formula over a stored lookup of the Company's name, and the title field; the views drop their Company column and sort by it | HAP's only text IF is in function formulas | implementation agent, accepted by planner |
| 15 Sep 2026 | Display Name is **read-only on a saved contact's form**, under Image, and not on the create form — rather than hidden | The retest found a hidden field dropped from every table, so Contacts and Archived lost their first column; an always-true rule hiding it on the form would be a workaround reviewers must learn | planner |

## Worksheet 02 · Units & Packagings

| Date | Decision | Why | By |
|---|---|---|---|
| 15 Sep 2026 | Menu group **Products** holds Units & Packagings, then Products and Product Variants | Units belong with the catalogue they measure | planner |
| 15 Sep 2026 | UN/ECE Code waits for e-invoicing; packaging Barcodes wait for Products | `unece_code` comes from `account_edi_ubl_cii`; barcodes live on products | planner |
| 15 Sep 2026 | Absolute Quantity is kept correct down the whole reference chain, though hidden as in Odoo | Every later unit conversion uses it | planner |
| 15 Sep 2026 | Seed Odoo's 30 standard units | Odoo ships them as data; Products needs them | planner |
| 15 Sep 2026 | Add a **recursion check** (hidden Parent Path + "Recursion Detected." rule) and Odoo's "Reference unit of measure is missing." rule | A reference loop (km → m → km) would make HAP recompute Absolute Quantity endlessly; both mirror checks Odoo enforces (`_parent_store`, `_check_factor`) | implementation agent, accepted by planner |
| 15 Sep 2026 | Sequence is a formula that follows Contains; lists sort by Sequence, then Unit Name | HAP rows cannot be dragged; Odoo sets Sequence once at creation and orders by `sequence, relative_uom_id, id` — close enough to review | implementation agent, accepted by planner |

## Worksheet 03 · Products

| Date | Decision | Why | By |
|---|---|---|---|
| 15 Sep 2026 | Products sits in menu group Products, **before** Units & Packagings | Odoo's catalogue order: products first, units under configuration | planner |
| 15 Sep 2026 | **Scope is the `product` module's own fields** plus Sales Description; Sales, Invoicing, Project, Purchase and Inventory fields on products are appended when those apps or bundles land | The bridge-pack rule: an app appends its fields to Products, it does not arrive pre-built | planner |
| 15 Sep 2026 | Product Type offers **Goods and Service**; Combo waits for the Product Combos bundle | A Combo product is meaningless without Combo Choices | planner |
| 15 Sep 2026 | Views open on a **gallery**, then List and Archived; favourites first, then by name | Odoo's Products action opens in Kanban; its order is `is_favorite desc, name` | planner |
| 15 Sep 2026 | Seed the 14 products; the monitor's, chair's and desk's variants wait for Product Variants (04) and its bundle | Without the Product Variants bundle each product has exactly one variant | planner |
| 15 Sep 2026 | Packagings leave out the product's own Unit; both unit pickers show Contains and Reference Unit; a negative Cost is refused in the form only; Weight and Volume default to 0 | Odoo's field domain, Odoo's unit dropdown, Odoo's onchange (not a constraint), and the tenant's data | implementation agent, accepted by planner |

## Worksheet 04 · Product Variants

| Date | Decision | Why | By |
|---|---|---|---|
| 15 Sep 2026 | **One variant per product** until the Product Variants bundle, created with its product; **Products is the master** of Internal Reference, Cost, Weight, Volume and Favorite, copied to the variant by workflows and read-only there | Without attributes every Odoo product has exactly one variant, whose fields are the product's (single-variant inverses, `is_favorite` related) | planner |
| 15 Sep 2026 | Variants show as **"[Internal Reference] Name"** — the title field, read-only, not hidden; products keep showing their Name | Odoo `_compute_display_name`; invoice lines will pick variants; hidden fields drop out of tables. Settles the open question for variants; " (attribute values)" waits for the bundle | planner |
| 15 Sep 2026 | The product → variant copy reaches **active variants only**; unarchiving a product revives only its **oldest** variant and copies the fields onto it | The tenant's invoice lines point at archived original variants (FURN-0001, FURN-0002) that 07 may add as archived extras: they must keep their own reference and cost. Odoo `_create_variant_ids` revives one variant | planner, with the implementation agent |
| 15 Sep 2026 | The variant's Archive / Unarchive also archive / unarchive the product when it has no other active variant | Odoo `action_archive` / `action_unarchive` on `product.product` | implementation agent, accepted by planner |
| 15 Sep 2026 | No Create, Duplicate or Re-create on Product Variants; Product is read-only | Odoo's variant action has `create: False`, the views `duplicate="false"`, and the form shows the product read-only | implementation agent, accepted by planner |
| 15 Sep 2026 | Views sort Favorite, then Internal Reference, then Name; variants without a reference come first | Odoo's list `default_order`; HAP cannot put empty values last | planner |
| 15 Sep 2026 | Sales Price and the product's other fields are lookups, and Variant Image is the variant's own | HAP lookups are read-only; Odoo's editing through `_inherits` and its image fallback are left out | implementation agent, accepted by planner |
| 15 Sep 2026 | Extra Packagings leave out the product's Unit by comparing unit names; the Sales and Inventory tab rules are Products' | A lookup of a relation stores the title, so no record id is available to compare; the variant form inherits the product form's tabs | implementation agent, accepted by planner |

## Worksheet 05 · Journals

| Date | Decision | Why | By |
|---|---|---|---|
| 15 Sep 2026 | Journals is assigned to **Teh Li Wei** as the reviewing colleague and is handed off for parallel work while Products is being built; implementation agents must not touch the Journals worksheet | Two builders can work in parallel without crossing the Products, Units & Packagings or Journals boundaries; later Invoices will relate to Journals | owner |
| 15 Sep 2026 | The Journals handoff records the Phase 1 core fields only; Chart of Accounts, Currency, Payments, e-invoicing and other Journal relations are deferred | Creating fake Text fields would produce the wrong model and make later bundles harder to attach correctly | owner / Teh Li Wei |
| 15 Sep 2026 | This commit is planning and collaboration handoff only; it does not build HAP objects and must not change `build/ids.json` | The correct CLI profile is not currently authenticated, and live Odoo UI extraction plus a later CLI preflight are required before creation | Teh Li Wei |
| 15 Sep 2026 | After the Products handoff, build the approved Journals core slice through the explicit `fbmy-nocoly` CLI profile and record its HAP IDs; keep the status at CLI-built until Web validation passes | The correct Nocoly organization/app were confirmed, Journals had zero records, and the work does not overlap Units & Packagings or Products | owner / Teh Li Wei |
| 15 Sep 2026 | Do not seed Journals or add deferred account/currency/payment relations; record the unavailable Sequence Prefix maximum-length setting instead of inventing a CLI parameter | The live Odoo source/form check and dependent Foundation bundles are still pending | Teh Li Wei |
| 15 Sep 2026 | Use the owner's authenticated `ohyes.odoo.com` tenant as the live UI source for Journals; do not require or join `casimir.odoo.com` for this worksheet | The owner explicitly supplied the correct tenant and its user-visible list/form are authoritative for this hand-off | Teh Li Wei |
| 15 Sep 2026 | Preserve the existing Communication Standard option IDs and default binding, but update their visible labels to include Odoo's example references | The `ohyes.odoo.com` Sales journal visibly shows `Full Reference (INV/2024/00001)`, `European (RF83INV202400001)` and `Numbers only (202400001)` | Teh Li Wei |
| 16 Sep 2026 | **We complete Journals** on top of Teh Li Wei's build, keeping his control ids, aliases and option keys; he keeps Invoices | The owner asked for it once his last Journals edit was 14 hours old; keeping the ids leaves his views and any later record valid | owner |
| 16 Sep 2026 | The gaps closed against casimir: prefix back to **5 characters and unique**, **Sequence** restored (default 10) with the views sorted Sequence → Type → Sequence Prefix, the two **dedicated sequence** checkboxes added, Payment Communications shown and required on Sales only, Archive / Unarchive buttons, a Type quick filter, Odoo's own help text, and the **7 tenant journals seeded** | Odoo `size=5`, `code_company_uniq`, `_order`, the form's `invisible` rules and the tenant's data | planner |
| 16 Sep 2026 | **Sequence is a visible, editable Number** | A HAP table has no drag handle, so the number is the only way to reorder journals | implementation agent, accepted by planner |
| 16 Sep 2026 | **Communication Type and Standard are required by a rule, not on the field** | A field a rule hides can never be filled: field-level required would make every non-Sales journal unsavable | implementation agent, accepted by planner |
| 16 Sep 2026 | **One flat form, no notebook tabs**; four interaction rules on Type decide what shows | Odoo's two pages would hold one and two fields until the deferred settings land; add the tabs when they have content | implementation agent, accepted by planner |
| 16 Sep 2026 | The 5-character prefix limit stays a **form-only check** | HAP does not enforce a text maximum on API writes and no filter operator measures length; No duplicates, which entry numbering depends on, is enforced everywhere | implementation agent, accepted by planner |
| 16 Sep 2026 | "You cannot archive a journal containing draft journal entries" waits for **06 Invoices**, then joins the Archive workflow | The check counts draft entries, which do not exist yet | implementation agent, accepted by planner |
| 16 Sep 2026 | **The archive guard is built, inside the Archive button's workflow**: count the Invoices whose Journal is the triggering journal and whose Status is Draft, and on one or more **stop before Active is written** | 06 Invoices now exists, so the check has something to count. Odoo `_check_auto_post_draft_entries`, `addons/account/models/account_journal.py:686` | owner |
| 16 Sep 2026 | The stop is a **中止流程 node (type 30) inside a branch path**, not an empty path | A HAP branch **converges**: both paths run into whatever follows the gateway, so a path that does nothing still reaches *Archive the journal*. Only an abort node stops the run. The existing update step was left exactly where it was — nothing moved, nothing deleted | implementation agent |
| 16 Sep 2026 | **The user is told by a 站内通知, not a dialog** — HAP has no dialog a button workflow can raise — and the message is Odoo's, verbatim, menus and all | §1 asked what a button workflow can actually put in front of a user. The answer: the button's confirmation (shown *before* the flow runs), and a notification node. So Odoo's text arrives in the notification list headed *Cannot archive this journal*, the click still reports "Operation completed", and the journal simply does not move. Recorded as difference 7 rather than worked around, as asked | implementation agent, for the owner |
| 16 Sep 2026 | The notification's recipient is the **系统 node's `triggeraid`**, not hap-cli's `kind: "triggerUser"` | That kind keys `uaid` off the trigger node, which on a *button* trigger the server reads back as **Last modifier** — the journal's last editor, who need not be the person clicking Archive | implementation agent |
| 16 Sep 2026 | Proved on **TEST draft guard** (TSTDG) and its own `TEST draft guard` draft entry, never on the tenant's journals | The Sales journal holds the two seeded drafts and would have proved the same thing, but archiving a real journal is not a test. Both TEST records stay as the reviewer's evidence | owner |
| 16 Sep 2026 | **Journals gets Odoo's two notebook tabs — Journal Entries and Advanced Settings — and a remark block in each** naming what Odoo shows there and which bundle or table brings it | The owner asked for the tabs even while the deferred settings leave them thin; the remark blocks tell a reviewer what is coming rather than leaving a bare tab | owner |
| 16 Sep 2026 | Static notes are HAP's **remark block (control type 10010)**, not a divider's description | A divider renders its description nowhere at all, so the first attempt was invisible in the UI test; the remark block holds HTML and renders it | planner |

## Worksheet 06 · Invoices

| Date | Decision | Why | By |
|---|---|---|---|
| 16 Sep 2026 | **One Invoices worksheet holds all seven move types** — Journal Entry, Customer Invoice, Customer Credit Note, Vendor Bill, Vendor Credit Note, Sales Receipt, Purchase Receipt — with Type as a plain dropdown, and three views dividing the one table | Odoo's `account.move` is one model behind seven menus; separate worksheets would mean two numbering schemes and two posting workflows. Settles the open question raised for 06 | owner |
| 16 Sep 2026 | The **Number is assigned by the Confirm workflow**, reproducing Odoo's `sequence.mixin`: `<journal Sequence Prefix>/<year of the Accounting Date>/<n padded to 5>`, with `R` + prefix for a credit note whose journal has a Dedicated Credit Note Sequence | The number is the link back to 05 Journals and the thing a reviewer checks first; a HAP auto-number field cannot restart per journal and per year | owner |
| 16 Sep 2026 | **The three tenant invoices are seeded together with their three customers in Contacts** — records only there, no field, view or rule touched | An invoice without its customer shows an empty picker; Contacts held only `TEST …` records, so these are the app's first real contacts | owner |
| 16 Sep 2026 | **Teh Li Wei's skeleton is taken over, keeping his 15 control ids**; three field labels and the empty remark block are renamed and the rest is added around them | Keeping the ids leaves any later record and anything he built on them valid, and the rename only moves a build note out of a label | owner |
| 16 Sep 2026 | **Invoices has no Active field, no Archived view and no Archive / Unarchive buttons** — the first worksheet in the app without them | `account.move` has no `active` field: a document is cancelled, never archived | implementation agent, accepted by planner |
| 16 Sep 2026 | **Number is the title field**, read-only, holding the word "Draft" until Confirm; Invoice Date is no longer the title | Odoo's h1 is the number, and a HAP title is what every picker, card and relation shows — an invoice picked on a line must read INV/2026/00001. Not *No duplicates*: several drafts share the word Draft | implementation agent, accepted by planner |
| 16 Sep 2026 | **The read-only rule was built as written.** A HAP interaction rule does have a read-only action (rule item type 4), so *A posted or cancelled document is closed for editing* locks the eight fields Odoo locks; nothing was faked with a hide rule | §1 made the rule conditional on HAP offering the action. Like every interaction rule it is browser-side only — the API still writes a posted document | implementation agent |
| 16 Sep 2026 | **The numbering counts, it does not reserve.** Two confirmations in the same second can produce the same number, and a Reset-to-Draft-and-Confirm-again takes a new one | A HAP workflow has no row lock, and Odoo's `sequence.mixin` takes one. Noted rather than built around, as §1 asked; both are listed as differences | implementation agent, accepted by planner |
| 16 Sep 2026 | The **Invoice Lines and MyInvois tabs carry a remark block and no divider**, unlike Journals' two tabs | §1's form layout names a divider only for Odoo's own two group headings inside Other Info, and each block already opens with a bold lead-in. A divider can be added in one save; removing one needs the owner's approval | implementation agent |
| 16 Sep 2026 | The nine skeleton controls are given **Odoo's field names as aliases**, and the worksheet's alias becomes `account_move` | Every one of them was empty, so nothing was overwritten, and `BUILDING.md`'s convention — aliases are Odoo field names, a worksheet's alias is its model — now holds across all six worksheets | implementation agent |
| 16 Sep 2026 | **Confirm dates an invoice or receipt only, not a plain journal entry** — the branch tests Type as well as the empty Invoice Date | Odoo `_post` dates `is_invoice(include_receipts=True)`; the UI test asked for the detail and it was one condition on a branch path that already existed. Replaces the difference the first build recorded | planner, built by the implementation agent |
| 16 Sep 2026 | **`selfcheck` leaves any TEST document that already carries a well-formed number alone**, whatever its Status, and confirms only unnumbered drafts | Re-running the step after the UI test took a number back off a cancelled document and re-issued it to another record — a live duplicate. The numbers a Confirm produces depend on what is already posted, so a repeatable check must not renumber what a test left behind | implementation agent |
| 16 Sep 2026 | **The INV/2026/00002 gap is left open** rather than closed by renumbering a TEST record | The gap is the UI test's own evidence for difference 3. It does mean the next Sales invoice confirmed will collide with INV/2026/00005 — flagged in §3 for the owner, not worked around | implementation agent, for the planner |
| 16 Sep 2026 | **The Number is one past the highest already issued, not one past a count** — a search step limited to one record, filtered on the same prefix and sorted Number descending | §1 specifies a count, which breaks as soon as a number is freed: the UI test's Reset to Draft left INV/2026/00002 unused and the next count-based Confirm would have re-issued a number a record already held. The zero-padded suffix makes a descending text sort numeric order within one prefix and year, so no new field is needed. Proved by TEST-SEQ-7 → INV/2026/00006 | planner, built by the implementation agent |
| 16 Sep 2026 | **Confirm assigns a Number only to a document that has none**, and leaves an existing one alone | Odoo `_post` sequences a move only when `name` is unset and `button_draft` keeps the name, so a reset document takes its old number back. It stops new gaps opening and removes the stale-Payment-Reference oddity at the same time. Difference 3 is retired | planner, built by the implementation agent |

## Worksheet 07 · Invoice Lines

| Date | Decision | Why | By |
|---|---|---|---|
| 16 Sep 2026 | **Invoice Lines is a worksheet of its own, mounted as the subtable of the Invoices tab *Invoice Lines*** — not a HAP child table built inside Invoices | Odoo's Journal Items are a standalone list as well as an invoice's lines. A mounted worksheet keeps its sidebar entry, its own view, its own rule and its own records; the `child_fields` kind of 子表 creates a hidden child table with none of them | implementation agent, as §1 asks |
| 16 Sep 2026 | **06's four amounts are written by two workflows, not replaced by a native roll-up.** HAP does have one — the 汇总 control, type 37 — but it is a different control type, so using it for Untaxed Amount would mean deleting `6aa9f847e54d2a34fa4dfef3` | §1 says stop and report rather than replace one of the four; deleting a field needs the owner's approval and 06 is out for review. A 汇总 would also only reach Untaxed Amount — Total is Untaxed + Tax and Amount Due is Total | implementation agent, reported to the planner |
| 16 Sep 2026 | **Two roll-up workflows, not one**: *added or changed* (新增或更新) and *deleted* (删除), with the same five steps | A HAP worksheet-event trigger takes one event; 2 and 3 cannot share a trigger | implementation agent |
| 16 Sep 2026 | The roll-up sums **Product lines only** — the filter tests Display Type as well as the invoice | Odoo's `amount_untaxed` sums `invoice_line_ids`; a section, a subsection or a note is not one. The rule only *hides* a section's figures, it does not null them, so without the condition a value typed before the switch would join the total | implementation agent |
| 16 Sep 2026 | **The Tax is read and never written.** Total = the roll-up + the invoice's own Tax; Amount Due = Total | §1: nothing in Phase 1 can compute a tax rate, and the Taxes bundle is recorded and ignored. It also keeps the tenant's documents reconciling exactly | owner's direction of 16 Sep, built as specified |
| 16 Sep 2026 | **Label is the title field**, reusing the new worksheet's stock title control; the other two stock controls are not carried into the first save | Odoo's `_rec_name` on `account.move.line` is `name`, and a HAP title is what every picker and card shows. Description and Attachment have no counterpart on a journal item — the same first-save pattern as 02, 03 and 04 | implementation agent |
| 16 Sep 2026 | The mounted subtable on Invoices is named **Lines** with its title hidden, and sits at row 8 under the remark block, pushing every control below it one row down | Odoo's tab shows the table with no heading of its own. "Invoice Lines" would clash with the tab of that name, which any name-keyed lookup resolves ambiguously. A HAP row holds 12 columns, so the remark block and a full-width table cannot share one — only `row` changed on 06's controls, nothing else | implementation agent |
| 16 Sep 2026 | **`nocoly/build/invoices.py` was changed**: its `PLACE` gained the subtable and the rows below it, `desired()` returns place only for a 子表, and its full save goes through the CLI's session | 06 owns the Invoices layout; without it `invoices.py check` reports an unknown control and `guard()` refuses to run. The session save is forced: a 子表's `relationControls` snapshot pushes `update-fields --controls` past the command-line limit. `invlines.py` reads `invoices.PLACE` and never writes it | implementation agent, for the planner |
| 16 Sep 2026 | The rule is **hide · Display Type is Section, Subsection or Note**, not *show · is Product* | A rule applies its action while its condition holds and the opposite when it does not, so the hide form leaves the six figure fields visible on a line whose Display Type has not been touched — which is what a line is for | implementation agent |
| 16 Sep 2026 | **Nothing was deleted, in the build or in the self-check.** `selfcheck` adds one `TEST roll-up line`, moves its Quantity twice and leaves it in place; a line *removed* is §3's test 13 | The owner's standing rule. The delete workflow is built, published and read back (`triggerId` 3), but only the UI test can fire the event | implementation agent, on the owner's constraint |
| 16 Sep 2026 | Lines are keyed on the **invoice's rowid**, never on its Number | Two of the three seeded documents are unnumbered drafts whose Number is the word "Draft"; a Number-keyed index merged their lines and the first seed run wrote one document's lines over another's | implementation agent |
| 16 Sep 2026 | **The delete roll-up was never broken.** `hap worksheet record delete` sends `triggerWorkflow: false` unless `--trigger-workflow` is passed, so every CLI deletion suppressed the workflow silently; with the flag the roll-up fires and the invoice follows | The delete workflow's run history was empty — zero instances — while the add/update one had sixteen. Both triggers are identical but for `triggerId` 2 / 3, both published and enabled, and no scope or source switch exists on either. Proved both ways on one `TEST delete probe` line. Nothing in the worksheet was changed | implementation agent, after the planner's UI test |
| 16 Sep 2026 | **A posted document's lines stay editable, and closing that is left to the owner** | 06's rule *A posted or cancelled document is closed for editing* names eight controls and the 子表 is not one — it did not exist when the rule was written. Odoo locks a posted invoice's lines. Adding the subtable control to that rule is a one-line change to `invoices.py`, but it is 06's rule and 06 is out for review. Recorded as difference 11 | implementation agent, for the owner |
| 16 Sep 2026 | **The owner took that change**: `Lines` (`6aaa2baae43d174ab3749de9`) is the ninth control in 06's read-only rule, from the end of Phase 1 | Odoo locks a posted move's lines and the difference was only ever an ordering accident — the rule predates the mount. One line in `invoices.py` (`CLOSED_FIELDS`); HAP stored the 子表 like any other target, unchanged | owner |
| 16 Sep 2026 | **Whether it locks anything is left open, not assumed.** 06 gains **test 25**; if the table is not visibly locked the control comes back out and difference 11 stands | An interaction rule is browser-side, so no CLI reading tells "greys the table" from "does nothing", and the API writes a posted document's lines either way. Nothing else in ERP Master — or in the brownfield Sales app — uses read-only on a 子表, so there was no precedent to read off. The owner runs the UI check | implementation agent, for the owner |
| 16 Sep 2026 | **It locks. The control stays, and 07's difference 11 is resolved** — a read-only rule **hides a 子表's row controls** | Test 25 in the browser: the posted INV/2026/00001 shows the Invoice Lines table with no *Add a row* and no *Batch Operation*, and the Display Type column's required asterisk gone; the Sunway Construction Group draft keeps both and stays editable. A posted document's lines are now closed for editing, as in Odoo. Still browser-side, like every interaction rule, so the seed and roll-up scripts are unaffected | owner, from the UI test |

## Phase 1 · Roles

Set on 16 Sep 2026, at the end of Phase 1. The table each role carries is in `REVIEWING.md` › *Phase 1 · Roles*;
the build is `build/roles.py` and the ids are in `build/ids.json` under `roles`.

| Date | Decision | Why | By |
|---|---|---|---|
| 16 Sep 2026 | **Roles are set once, for the whole app, at the end of Phase 1** | Every worksheet's *Not built now* deferred them here: a role's rules name worksheets, so they can only be written once every worksheet exists | owner |
| 16 Sep 2026 | **HAP's five stock roles are renamed to English** — 管理员 · 运营者 · 开发者 · 成员 · 只读 become Administrator · Operator · Developer · Member · Read-only. Only the label changes; `roleType`, the permission level and the members are untouched | The house rule of 15 Sep: names and labels in English, as in Odoo. Renaming is a read-modify-write of the whole role model with only `name` replaced, so nothing else can move | owner |
| 16 Sep 2026 | **Four business roles, one per Odoo accounting group**, each recorded against its group in the role's own description: Accounting Administrator (`account.group_account_manager`, Odoo's *Administrator*) · Accountant (`account.group_account_user`, *Show Full Accounting Features*) · Invoicing (`account.group_account_invoice`, *Invoicing*) · Accounting Read-only (`account.group_account_readonly`, *Show Accounting Features - Readonly*) | Odoo's own four accounting groups, verified against `addons/account/security/account_security.xml` in this checkout rather than from memory. The description is where a reviewer sees which Odoo group a HAP role stands for | owner |
| 16 Sep 2026 | **Delete goes to Accounting Administrator alone**; every other role's delete scope is none | Odoo's `group_account_manager` is the only accounting group with unrestricted rights; deleting a posted document is not something an accountant or an invoicing clerk does | owner |
| 16 Sep 2026 | **Journals is read-only for Invoicing** where it is editable for Accountant | Odoo: `group_account_invoice` "can create/edit invoices, refunds, payments, etc but cannot see accounting related stuff" — journals are configuration | owner |
| 16 Sep 2026 | **No member is assigned to any of the four**, and Casimir and Teh Li Wei stay app administrators exactly as they were | Who belongs in which role is the owner's call, and a role with no member changes nothing until someone is put in it | owner |
| 16 Sep 2026 | Everything a role can be given **beyond read / add / edit / delete** — view sharing, import, export, batch operations, record sharing, printing, the record log — is left at **hap-cli's defaults**, the same for all four | The owner's table speaks to the four record rights only. Guessing at the rest would put settings in front of a reviewer that nobody asked for; they are written down in `REVIEWING.md` instead, so a real preference can replace them | implementation agent, for the owner |
| 16 Sep 2026 | **Export is the one exception, and it goes to Accounting Administrator alone**, recorded against **`base.group_allow_export`** in the role's description and in `REVIEWING.md` | Odoo keeps exporting behind a group of its own, granted deliberately and not implied by an accounting level — so hap-cli's default of no export is *faithful* for the other three rather than an oversight, and saying which Odoo group it stands for stops the next reader reading it as one. Answered the flag the build raised | owner |
| 16 Sep 2026 | An existing role's description and export right are changed by a **read-modify-write of the whole `appRoleModel`** (GetRoleDetail → EditAppRole), not by re-creating the role | `create-fine` only creates, and V3 has no call that edits a role afterwards. It is the path the Roles page itself takes and the one `role rename` already uses, and posting the model back unchanged but for those two keys is what makes the scopes, members, field and view permissions provably immovable | implementation agent |
| 16 Sep 2026 | Field and view permissions are left **fully allowed**; the **record scope** is what restricts | That is how HAP's fine-grained role works — a "view" worksheet reads every field back as editable and the record's edit scope of none is the gate. A per-field lock would say the same thing twice and would have to be rewritten every time a worksheet gains a field | implementation agent |
| 18 Sep 2026 | **Countries follows Odoo's own access list, not this app's pattern**: the four business roles get **view**, and only the app **Administrator** creates, edits or deletes a country — Accounting Administrator is *not* full, the first worksheet where it is not | `odoo/addons/base/security/ir.model.access.csv` gives `group_public`, `group_portal`, `group_user` and `group_partner_manager` read alone on `res.country`, and read-write-create-delete to `base.group_system` — the settings administrator — which none of the four accounting roles stands for. Odoo's own Countries list carries `create="0" delete="0"` for the same reason: the list ships with the database and is not business data. The owner was shown both readings and chose Odoo's ("Follow Odoo's pattern"). **`res.country.state` is different** — there `group_partner_manager` has all four rights, so bundle 5 will not inherit this | owner |
| 18 Sep 2026 | **States follows Odoo's access list too — and lands somewhere else than Countries**: Accounting Administrator *full*, **Accountant and Invoicing *view · add · edit***, Accounting Read-only *view*; delete stays with Accounting Administrator alone | `ir.model.access.csv` gives `res.country.state` to **`base.group_partner_manager`** — a contact manager — with all four rights, where `res.country` needs `base.group_system`, a settings administrator; Odoo's own Fed. States list carries a **New** button where its Countries list does not. So the roles that create a contact here create the state that contact sits in, which is the same cell each already has for Contacts. The owner ruled mid-build: *"Follow Odoo's for role-based details"* — the same instruction that settled Countries the other way | owner |
| 18 Sep 2026 | **The inert State key formula is deleted, and Odoo's `unique(country_id, code)` is enforced instead on a Text control kept by a workflow**, with a read-only **Duplicate code** checkbox as what a person sees | HAP takes *No duplicates* on a function formula and enforces it nowhere — neither an API write nor a form save was refused (12 §3 test 7) — so the field promised a check it did not make. On a **Text** control the same switch is real (bundle 4: refused in the form and with `resultCode 11` through the API), so the key moves there and a workflow writes it. HAP still cannot *refuse* the save the way a database constraint does, because a workflow runs after it; what it can do is catch the duplicate and name it on the record. The owner: *"Drop it. Enforce uniqueness via other methods, maybe code+country_code or something else"* | owner |
| 18 Sep 2026 | **Taxes takes Chart of Accounts' roles row unchanged** — Accounting Administrator *full*, the other three *view* — and needed no ruling, where Countries and States each needed one | `addons/account/security/ir.model.access.csv` gives `account.tax` (rows 74–77) exactly what it gives `account.account` (rows 68–72): `account.group_account_manager` reads, writes, creates and deletes; `base.group_user`, `group_account_readonly` and `group_account_invoice` read and no more. Odoo's *menu* is stricter still — Configuration ▸ Accounting is `groups="account.group_account_manager"` — but every internal user can read a tax, which is what lets an invoice line show one, and Chart of Accounts sits under the same menu with the same answer | planner, from Odoo's own file |
| 18 Sep 2026 | **Tax Group is a Dropdown of the tenant's ten group names, not a worksheet of its own** — `account.tax.group` goes into *Not built now* | The field is required on a tax and sits on a tab we do build, so leaving it out would make the tax unfaithful; but Odoo hides the Tax Groups menu behind **`base.group_no_one`**, so it is a developer-mode screen no user reaches, and the tenant's ten groups **differ only in the name** — same country, same sequence 10, same SST Payable and SST Receivable, no advance account, no preceding subtotal. A Dropdown carries everything that varies. If the model is ever wanted, the Dropdown becomes a Relation the way Contacts' Country and State did in bundles 4 and 5 — a path already walked twice | planner |
| 18 Sep 2026 | **Odoo's five-way tax-name constraint is enforced by bundle 5's key-and-mark pattern**, with a four-part key `<country>\|<type>\|<scope>\|<name>` on a hidden Text control, a *Duplicate name* checkbox and a quiet workflow ending in an abort | `_constrains_name` is unique on (company, name, Tax Type, Tax Scope, Country) and skipped for Tax Type *None* — which is why *0% NA* exists twice and eight of the 27 names are shared. HAP's *No duplicates* is one field, real only on a Text control a person types into, and **ignored by a workflow's own write** (12, 18 Sep). So the machinery the owner approved for States carries over unchanged, ceiling included: a duplicate saves, then loses its key and is marked. Proved in the browser the same day (13 §3 test 13) | planner, following the owner's ruling of 18 Sep on States |
| 18 Sep 2026 | **The invoice's Tax is Σ (the lines' Total) − Σ (their Subtotal), and no per-line tax figure is invented** | Odoo has no tax column on `account.move.line` — it has `price_subtotal` and `price_total`, and writes its tax *lines* separately. Building `price_total` and subtracting gives the same number **exactly**, because each line's Total is itself rounded to two decimals and the company rounds per line (`tax_calculation_rounding_method = round_per_line`). It also kept the change to the two roll-up workflows small: one more aggregate node, one more formula, one more field on the update | planner |
| 18 Sep 2026 | **Only a Percentage tax contributes to a line's rate** — the 汇总 that sums the taxes' Amount is filtered to Tax Computation *Percentage* | A Fixed tax would need `quantity × amount` and a Percentage-Tax-Included one `amount ÷ (1 − amount ÷ 100)`; three different formulas cannot come out of one sum. Filtering means such a tax adds **nothing** rather than something wrong, and on this tenant nothing is lost: **every active tax is a Percentage**, and the Fixed and Custom Formula ones are all archived. Recorded as difference 4 of 13 §3 rather than worked around | planner |
| 18 Sep 2026 | **`advancedSetting.showtype` on a HAP Dropdown is the display style, and `"2"` is the progress bar** — the five Taxes dropdowns were built with `"2"` and drew as sliders in the table; all five were set to `"0"` during the UI test | Every other Dropdown in the app — thirteen of them — carries `"0"`, and Chart of Accounts' Type renders as plain text as Odoo's does. `"2"` is not "a dropdown list" as the builder's comment claimed; that meaning belongs to a **Relation**, where 1 · 2 · 3 are list, tab and dropdown. Fixed rather than reported, because it was one value and the app's own precedent settled it (13 §3 test 22) | planner |
| 21 Sep 2026 | **Odoo is the only reference, for every worksheet and whoever builds it** — not the ohyes app, not either person's earlier build | Teh's nine CRM worksheets were built against the live *ohyes* Opportunity UI (its Sales Teams are seeded with `info@ohyes.odoo.com`), while our fourteen were built against casimir's Odoo. Two references means two answers to the same question and a reconciliation that grows every week. The owner: *"Just against Odoo. It should be general, and not specific to either of us."* So ERP Master is a faithful Odoo, and anything ohyes does differently is a client customisation that belongs in a pruned copy, not in the master | owner |
| 21 Sep 2026 | **Where our build and Odoo differ on a detail, Odoo wins** — applied first to the two open calls from bundle 6: a product's tax pickers **exclude archived taxes**, and an invoice line's **keeps** them; and `products.py`'s layout spec is re-cut to **Odoo's own product form** rather than to whatever the live form had drifted to | Odoo's `product.template.taxes_id` carries `domain=[('type_tax_use','=','sale')]` on an `active_test`-on field, so an archived tax is not offered; `account.move.line.tax_ids` carries `context={'active_test': False}`, so it deliberately is. The same rule settles the Products layout: the drift was never a decision about whose edits to adopt — it is simply a spec that stopped matching Odoo. The owner: *"For those two small calls, follow whichever used by Odoo"* | owner |

## Worksheet 18 · CRM Activities

| Date | Decision | Why | By |
|---|---|---|---|
| 21 Sep 2026 | **Activities stays a CRM-scoped worksheet instead of reproducing Odoo's global polymorphic engine** | The immediate value is salesperson follow-up on Leads; extending one activity model across Contacts, Orders, Invoices and future apps would turn this slice into a cross-app platform project | owner / Teh Li Wei |
| 21 Sep 2026 | **The mounted Activities subtable is hidden from the Lead form; `My Activities` is the user-facing entry** | Odoo schedules through an activity action and does not present a normal child table on the Opportunity form | owner / Teh Li Wei |
| 21 Sep 2026 | **Activity creation, reassignment and due date produce in-app notifications** | An Activity must have operational effect, not merely store rows. The due reminder is a 09:00 per-record Due Date trigger guarded by Scheduled and Active | owner / Teh Li Wei |
| 21 Sep 2026 | **The direct `Schedule Activity` Lead button is not kept after Web failure** | CLI retained a valid-looking child relation wire, but the rendered action rejected its hidden bridge as deleted/hidden. Leaving the button would knowingly ship a broken user path | Teh Li Wei |
| 21 Sep 2026 | **A future top button must use technical input fields plus Fill/Create workflows** | That architecture no longer depends on HAP's direct-related-record action seeing the hidden mounted subtable. It is a separate approved slice and requires another Web validation pass | Teh Li Wei |
| 21 Sep 2026 | **The approved Fill/Create replacement is now the Lead scheduling path** | Five hidden Lead inputs feed a published Create Activity node. CLI triggering created a correctly related Scheduled Activity, and Web confirmed the Lead dialog plus Calendar entry without submitting through Web | Teh Li Wei |
| 21 Sep 2026 | **Keep the HAP Fill dialog's rendered `Confirm` CTA and record the mismatch** | The action stores `sureName=Schedule`, but HAP renders `Confirm`. The creation flow works and the difference is presentation-only, so it is documented rather than replaced with a Web-only manual setting or an unverified raw workaround | Teh Li Wei |
| 21 Sep 2026 | **Reset scheduling inputs after every created Activity** | On an existing Lead, clearing the inputs does not cause the Fill dialog to reapply field defaults. The published workflow therefore restores To-Do, current date and trigger user and clears only Summary/Note, so repeat scheduling opens in a safe Odoo-like state | Teh Li Wei |
| 22 Sep 2026 | **Meeting uses a separate `Schedule Meeting` Lead action** | CLI could store one conditional dialog, but Web showed that its Business Rules did not dynamically reveal Meeting fields and one intermediate shape rendered blank. Two explicit actions are the verified user path | Teh Li Wei |
| 22 Sep 2026 | **A Meeting Activity is delivered before Calendar/Event parity** | The workflow creates a real Meeting Activity and preserves start/end, attendees, location, reminder and video URL, but hap-cli 0.8.31 does not expose the UI-only Collaboration Create Event node. Calendar creation and generated meeting links remain deferred, not simulated | Teh Li Wei |
| 22 Sep 2026 | **Activity Plan configuration stays separate from plan execution** | Plan/Step worksheets and assignment rules exist, but the CLI has no supported Loop Process DSL and the app has no proven type-29 wire. No non-functional Launch Plan button is shipped; a UI-created minimal loop template is the next execution gate | Teh Li Wei |
